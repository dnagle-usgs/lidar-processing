#
# Test files against the md5sum.txt file located
# at the root of each data directory.  Only show
# the failed files.


echo "This may take several minutes or even an hour..."
md5sum -c md5sum.txt | grep FAILED

